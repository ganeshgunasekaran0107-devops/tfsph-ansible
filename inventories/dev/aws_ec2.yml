---
plugin: amazon.aws.aws_ec2

regions:
  - ap-south-1

filters:
  instance-state-name: running
  tag:project: tfsph
  tag:Environment: dev

hostnames:
  - private-ip-address

keyed_groups:
  - key: tags.Role
    prefix: ""
    seperator: ""

compose:
  ansible_host: private_ip_address