---
plugin: amazon.aws.aws_ec2

regions:
  - ap-south-1

filters:
  instance-state-name: running
  tag:project: TFSPH
  tag:Environment: DEV

hostnames:
  - private-ip-address

keyed_groups:
  - key: tags.name
    prefix: tag_name
    seperator: "_"

compose:
  ansible_host: private_ip_address